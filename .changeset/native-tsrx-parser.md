---
"@tsrx/core": patch
"@tsrx/react": patch
---

Parse tags and bare fragments as native TSRX by default, remove `component` keyword parsing, and compile React function components that return native TSRX.
