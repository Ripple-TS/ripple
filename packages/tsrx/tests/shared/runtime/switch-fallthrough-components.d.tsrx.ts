/**
 * Declarations for the `.tsrx` fixture next to this file. Each target package
 * compiles the fixture with its own bundler plugin, so from TypeScript's point
 * of view its exports are opaque component functions.
 */
export declare function AppSwitchIsolated(props: { status: string }): unknown;
export declare function AppSwitchEmptyCase(props: { status: string }): unknown;
